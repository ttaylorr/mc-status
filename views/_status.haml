%section
  .row
    - locals[:services].each do |service|
      .col-md-3
        .alert{:class => service.to_bootstrap_name, :data => {:service_name => service.api_name}}
          = "<b>#{service.display_name}</b> is #{service.humanize_status}"
